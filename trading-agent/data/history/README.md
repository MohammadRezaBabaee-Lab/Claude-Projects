Place one CSV per symbol here: `SPY.csv` with columns `date,open,high,low,close,volume` and
optionally `adj_close`. Generate them with `python scripts/download_history.py --symbols SPY --start 2018-01-01`.
Data files are git-ignored except this README.
