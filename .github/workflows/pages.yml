name: pages

on:
  push:
    branches: [main]
    paths: ["index.html", "demo/**", ".github/workflows/pages.yml"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Assemble the site
        run: |
          mkdir -p _site/demo
          cp index.html _site/
          cp -r demo/index.html demo/data demo/preview.png _site/demo/
          touch _site/.nojekyll
          find _site -type f | sort

      # enablement turns Pages on for the repository the first time this runs,
      # so publishing does not depend on someone visiting Settings first.
      - uses: actions/configure-pages@v5
        with:
          enablement: true
      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site
      - id: deployment
        uses: actions/deploy-pages@v4
